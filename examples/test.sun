lib [math]
pout("Select the operation type")

@'selector > 0 && selector < 3'
{
selector = pin()
pout("Type 1 to make an addition\nType 2 to make a su
'n == 1' ->
pout("Type both numbers you want to add\n")
n = pin()
p = pin()
number result = n + p
return result
otherwise 'n == 2' ->
pout("Type both numbers you want to subtract)
	string n = pin()
	string p = pin()
	result = n - p
otherwise 
	pout("Invalid operation")
return result
}